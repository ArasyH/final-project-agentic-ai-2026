 # UI
  streamlit>=1.35,<2.0

  # LLM & framework
  groq>=0.9
  langchain>=1.0
  langchain-core>=1.0 
  langchain-community>=0.4
  langchain-groq>=1.0
  langchain-huggingface>=1.0
  langchain-chroma>=1.0

  # RAG / vector store
  chromadb>=1.0
  sentence-transformers>=5.0
  transformers>=4.50
  huggingface-hub>=0.30

  # PyTorch — install dari PyPI default (torch>=2.x sudah CPU-friendly untuk Linux x86_64)
  # Extra-index-url PyTorch dihapus karena `uv` (dipakai Streamlit Cloud) conflict
  # dengan requests==2.28.1 di index tsb vs requests>=2.32.5 yg dibutuhkan langchain.
  torch>=2.0
  
  # Observability
  langfuse==3.14.6

  # API server (dependency transitif app/)
  fastapi>=0.115
  pydantic>=2.10
  pydantic-settings>=2.10

  # ETL & utility
  python-dotenv>=1.0
  requests>=2.30
  APScheduler>=3.10
  numpy>=1.24
  pandas>=2.0